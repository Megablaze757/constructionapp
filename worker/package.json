{
  "name": "builderos-quoting-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BuilderOS API — Cloudflare Worker + D1 + Groq",
  "scripts": {
    "dev": "wrangler dev",
    "stub": "node dev/stub-groq.js 8799",
    "ai": "node dev/ai-worker-server.mjs 8790",
    "web": "python3 -m http.server 8788 --bind 127.0.0.1 --directory ../web",
    "test": "node --test \"test/*.test.js\"",
    "e2e": "node e2e/run-all.mjs",
    "build:generated": "node scripts/build-generated.mjs",
    "db:local": "wrangler d1 execute builderos-quoting --local --file=migrations/0001_init.sql && wrangler d1 execute builderos-quoting --local --file=migrations/0002_seed.sql && wrangler d1 execute builderos-quoting --local --file=migrations/0003_phase_d.sql && wrangler d1 execute builderos-quoting --local --file=migrations/0004_phase0.sql && wrangler d1 execute builderos-quoting --local --file=migrations/0005_phase1.sql && wrangler d1 execute builderos-quoting --local --file=migrations/0006_phase3.sql",
    "db:remote": "wrangler d1 execute builderos-quoting --remote --file=migrations/0001_init.sql && wrangler d1 execute builderos-quoting --remote --file=migrations/0002_seed.sql && wrangler d1 execute builderos-quoting --remote --file=migrations/0003_phase_d.sql && wrangler d1 execute builderos-quoting --remote --file=migrations/0004_phase0.sql && wrangler d1 execute builderos-quoting --remote --file=migrations/0005_phase1.sql && wrangler d1 execute builderos-quoting --remote --file=migrations/0006_phase3.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "playwright": "^1.56.1",
    "wrangler": "^4.42.0"
  }
}
